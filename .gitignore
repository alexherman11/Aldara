node_modules/
dist/
.env
*.tsbuildinfo

# Generated artifacts from local agent runs
recordings/
scenarios/

# Vite build output for the React frontend
web/dist/

# Local-only env files
.env.local

# Self-hosted livekit-server binary (downloaded, see README)
tools/livekit/livekit-server.exe
tools/livekit/LICENSE

# Local dev process logs (`npm run dev` redirects stdio to these)
*.log
*.log.err
.logs/
.scenario.log

# Claude Code worktrees — never committed
.claude/worktrees/

# Smoke-test artifacts
chirp-out.wav

# Screenshots captured by the screenshot skill (.claude/screenshots/<ts>-<route>.png)
.claude/screenshots/

# dev-stack runtime state (PIDs of detached processes, hook sentinels)
.claude/.dev-stack.pids.json
.claude/.last-typecheck-web

# Per-user settings (settings.json is shared, settings.local.json is personal)
.claude/settings.local.json
